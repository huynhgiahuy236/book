# Local ebook storage retired

CapstoneBook không còn đọc PDF từ thư mục này. PDF được lưu trong Cloudflare R2 private bucket và truy cập qua `R2BookStorageProvider`.

Thư mục chỉ được giữ lại để lịch sử Git không gây nhầm lẫn; không đặt PDF mới tại đây. Xem `docs/R2-PDF-READER.md`.
